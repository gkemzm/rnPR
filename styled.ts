import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: { backgroundColor: '#cccccc', marginBottom: 32 },
  headerTitle: {
    textAlign: 'center',
    paddingVertical: 16,
    color: 'white',
    fontSize: 20,
    fontWeight: 'bold',
  },
  footer: {
    marginTop: 32,
  },
  footerTitle: {
    textAlign: 'center',
  },
});

export default styles;
